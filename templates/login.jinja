{% extends "layout.jinja" %}

{% block content %}
  <main class="container signup-section">
    <div class="section-content">
      <h2>Login</h2>

      <form method="POST" action="">
        
        <div class="form-group">
              <p class="error-message"></p>
        </div> 

        <div class="form-group">
            <label for="id">identifiant</label>
            <input type="text" required autofocus  id="identifiant" value="{{ request.form.username }}" name="username" placeholder="Name">
        </div>

        <div class="form-group">
            <label for="id">password</label>
            <input type="password" required autofocus id="svngoku" value="{{ request.form.password }}" name="password" placeholder="🚷🚷🚷🚷">
        </div>

         <button class="btn-primary" type="submit">login</button> 
      </form>
    </div>
  </main>
{% endblock %}